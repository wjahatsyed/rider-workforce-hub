server:
  port: 8071

spring:
  application:
    name: rider-service

  datasource:
    url: jdbc:postgresql://localhost:5432/rider_db
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
